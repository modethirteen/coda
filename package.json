{
  "private": true,
  "sideEffects": false,
  "type": "module",
  "packageManager": "yarn@4.1.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspaces foreach -At run build",
    "tsc": "yarn workspaces foreach -At run tsc"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
